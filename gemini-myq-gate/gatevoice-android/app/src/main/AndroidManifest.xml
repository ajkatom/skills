<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MICROPHONE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.QUERY_ALL_PACKAGES"
        tools:ignore="QueryAllPackagesPermission" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/Theme.Material3.DayNight">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Invoked by the QS tile, the widget, or "open GateVoice listen": listens once. -->
        <activity
            android:name=".ListenActivity"
            android:exported="true"
            android:excludeFromRecents="true"
            android:theme="@style/Theme.Translucent"
            android:label="@string/listen_label">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>

        <!-- Mic-free trigger for app shortcuts / "Hey Google" / deep links. -->
        <activity
            android:name=".ActionActivity"
            android:exported="true"
            android:excludeFromRecents="true"
            android:theme="@style/Theme.Translucent"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="gatevoice" android:host="act" />
            </intent-filter>
        </activity>

        <!-- Launcher entries per device, so "Hey Google, open GateVoice Gate"
             (a reliable app-open command) taps that door. Pair with a Google
             Home routine to map a natural phrase to it. -->
        <activity-alias
            android:name=".alias.OpenGate"
            android:targetActivity=".ActionActivity"
            android:exported="true"
            android:icon="@drawable/ic_launcher"
            android:label="GateVoice Gate">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <meta-data android:name="device" android:value="Gate" />
            <meta-data android:name="action" android:value="open" />
        </activity-alias>

        <activity-alias
            android:name=".alias.OpenDoor1"
            android:targetActivity=".ActionActivity"
            android:exported="true"
            android:icon="@drawable/ic_launcher"
            android:label="GateVoice Door 1">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <meta-data android:name="device" android:value="Garage Door 1" />
            <meta-data android:name="action" android:value="open" />
        </activity-alias>

        <activity-alias
            android:name=".alias.OpenDoor2"
            android:targetActivity=".ActionActivity"
            android:exported="true"
            android:icon="@drawable/ic_launcher"
            android:label="GateVoice Door 2">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <meta-data android:name="device" android:value="Garage Door 2" />
            <meta-data android:name="action" android:value="open" />
        </activity-alias>

        <activity-alias
            android:name=".alias.OpenDoor3"
            android:targetActivity=".ActionActivity"
            android:exported="true"
            android:icon="@drawable/ic_launcher"
            android:label="GateVoice Door 3">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <meta-data android:name="device" android:value="Garage Door 3" />
            <meta-data android:name="action" android:value="open" />
        </activity-alias>

        <!-- Mic-free keep-alive so the accessibility service stays connected
             when the UI is closed (and the wake word is off). -->
        <service
            android:name=".KeepAliveService"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="Keeps the accessibility service alive so voice/shortcut commands can press the myQ app when GateVoice is not open." />
        </service>

        <receiver
            android:name=".BootReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <!-- Always-on offline wake-word listener. -->
        <service
            android:name=".VoskWakeService"
            android:exported="false"
            android:foregroundServiceType="microphone" />

        <!-- Taps the myQ app on our behalf. -->
        <service
            android:name=".MyqAccessibilityService"
            android:exported="false"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/accessibility_service_config" />
        </service>

        <!-- Quick Settings tile: one tap -> listen. -->
        <service
            android:name=".GateTileService"
            android:exported="true"
            android:icon="@drawable/ic_launcher"
            android:label="@string/tile_label"
            android:permission="android.permission.BIND_QUICK_SETTINGS_TILE">
            <intent-filter>
                <action android:name="android.service.quicksettings.action.QS_TILE" />
            </intent-filter>
        </service>

        <!-- Home-screen widget: one tap -> listen. -->
        <receiver
            android:name=".GateWidgetProvider"
            android:exported="true">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/gate_widget_info" />
        </receiver>

    </application>
</manifest>
